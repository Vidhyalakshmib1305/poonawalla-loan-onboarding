# ── Web Framework ─────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# ── AI / LLM ──────────────────────────────────────────────────────────────
groq==0.9.0                    # Groq API client (LLM extraction, document OCR, chatbot)
openai-whisper==20231117       # Whisper STT for video call transcription

# ── Face Verification ─────────────────────────────────────────────────────
deepface==0.0.93               # DeepFace Facenet512 face matching + age estimation
tf-keras==2.16.0               # TensorFlow/Keras backend for DeepFace

# ── Document / Image Processing ───────────────────────────────────────────
pytesseract==0.3.10            # Tesseract OCR fallback for document extraction
pdf2image==1.17.0              # PDF to image conversion (Poppler required on host)
Pillow==10.3.0                 # Image manipulation
opencv-python-headless==4.9.0.80  # Video frame extraction, CLAHE preprocessing

# ── Data / Compute ────────────────────────────────────────────────────────
numpy==1.26.4
scipy==1.13.0                  # Statistical utilities (bureau simulation)

# ── HTTP / Networking ─────────────────────────────────────────────────────
requests==2.32.3               # HTTP calls from backend (Groq REST, chatbot proxy)
httpx==0.27.0                  # Async HTTP client

# ── Config / Utils ────────────────────────────────────────────────────────
pydantic==2.7.1
python-dotenv==1.0.1
aiofiles==23.2.1